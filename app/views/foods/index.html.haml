%h1 All Foods

%table.table.table-striped
  %thead.thead-light
    %tr
      %th Name
      %th Supplier
      %th Storage Location
      %th Treat?
      %th{ colspan: "3" } Actions

  %tbody
    - @foods.each do |food|
      %tr
        %td= food.name
        %td= food.supplier
        %td= food.storage_location
        %td= food.treat ? 'Yes' : 'No'
        %td= link_to 'View Details', food, class: "btn btn-primary btn-sm"
        %td= link_to 'Edit', edit_food_path(food), class: "btn btn-warning btn-sm"
        %td= button_to 'Remove', food, method: :delete, form: { data: { turbo_confirm: 'Are you sure?' } }, class: "btn btn-danger btn-sm"

%br

= link_to 'New Food', new_food_path, class: "btn btn-success"

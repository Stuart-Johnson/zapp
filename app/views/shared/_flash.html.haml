#flashes
  - flash.each do |key, value|
    .flash-message.alert.alert-dismissible.fade.show{ class: flash_class(key), role: 'alert' }
      %div= value
      %button.btn-close{"aria-label" => "Close", "data-bs-dismiss" => "alert", type: "button"}
